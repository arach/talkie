Start with the smallest surface that answers the task.
Use the mounted config files when the change is declarative.
Use the memo and workflow guides plus the Tools scripts when the task is about real app data.
For workflow creation, prefer the flat JSON format, adapt the closest bundled template, and save the workflow into Live Config/workflow-user.
Stay inside the generated workspace unless the task clearly requires reading more of the repo.
Use CONFIGURATION_GUIDE.md as a map, but treat code and live config files as canonical.
If you suggest a rule or workflow change, include one example that should pass afterward.
