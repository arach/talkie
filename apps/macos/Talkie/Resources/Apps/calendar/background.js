/**
 * Calendar Widget - background.js
 *
 * Minimal background script for the calendar widget.
 * Most logic lives in widget.html (runs in WKWebView with talkie.data bridge).
 */

(function() {
    'use strict';
    console.log('[Calendar] Background script loaded');
})();
